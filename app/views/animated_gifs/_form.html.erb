<%= bootstrap_form_for(animated_gif, label_errors: true) do |form| %>

  <%= render 'shared/error_messages', object: form.object %>

  <%= form.text_field :title %>

  <%= form.text_area :description %>
  
  <%= form.submit %>

<% end %>
